import { BarcodeNotExistValidatorDirective } from './barcode-not-exist-validator.directive';

describe('BarcodeNotExistValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BarcodeNotExistValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
