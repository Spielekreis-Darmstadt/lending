import { BarcodeExistValidatorDirective } from './barcode-exist-validator.directive';

describe('BarcodeExistValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new BarcodeExistValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
